{% extends "layout.html.twig" %}
{% block title %}投稿のリスト{% endblock %}

{% block body %}
    <h1>投稿のリスト</h1>
    <ul>
        {% for item in items %}
        <li>
            <a href="{{path('coffee_item', {'id': item.id}) }}">
                {{ item.title }}
            </a>
        </li>
        {% endfor %}
    </ul>
{% endblock %}
